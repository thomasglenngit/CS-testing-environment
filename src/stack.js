export default class stack {
  constructor () {
    this.elements = []
  }

  addElement(element) {
    return this.elements.push()
  }

  removeElement() {
    return this.element.pop();
  }
}

// bookStack = new Stack();

//bookStack.elements